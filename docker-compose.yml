services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: typing_app
      POSTGRES_USER: typing_app
      POSTGRES_PASSWORD: typing_app
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data:
