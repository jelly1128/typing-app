# 開発工程 — ウォーターフォール型フロー

## 全体像

```
P0 プロジェクト計画
     ↓
P1 要件定義 ───────── ゲート① doc-reviewer
     ↓
P2 基本設計 ───────── ゲート② doc / test / ops reviewer
     ↓
P2.5 アーキ検証(Walking Skeleton → 公開 URL)
     ↓
P3 詳細設計 ───────── ゲート③ doc / test / ops reviewer
     ↓
P4 テスト設計
     ↓
P5 実装
     ↓
P6 テスト実施
     ↓
P7 リリース
     ↓
P8 振り返り
     ↓
P9 (任意)多言語移植
```

**前工程のゲートが通るまで次工程の成果物を書かない。** これがウォーターフォールの本質。

---

## 1回のセッションの流れ

```
┌──────────────────────────────────────────────────┐
│  1. 開始   /ty-plan   (15分)                      │
│     git 確認 → 現在地 → 前回の打ち手 → 見積もり      │
│     ↓                                            │
│  2. 作業              (2〜2.5時間)                 │
│     その工程の成果物を作る                           │
│     ↓                                            │
│  3. ゲート  /ty-gate   (工程の最終回のみ)            │
│     完了条件と成果物の照合 → レビュー                │
│     ↓                                            │
│  4. 終了   /ty-end    (15分)                      │
│     実績記録 → ズレ分析 → 次回の打ち手 → コミット     │
└──────────────────────────────────────────────────┘
```

**セッションの境界を git で可視化する。** `/ty-plan` が git を読み、`/ty-end` が git に書く。これにより「どこまでが確定済みか」が常に `git log` から判断でき、複数セッションが同じリポジトリを触っても事故に気づける。

---

## 工程詳細

### P0 プロジェクト計画
- **目的:** 何をどの順でどれだけの時間で作るかを決める
- **成果物:** `charter.md` / `wbs.md` / `estimate-actual.md` / `progress.md` / `workflow.md` / `CLAUDE.md`
- **完了条件:** 全工程の WBS とタスク別見積もり時間が1枚にあり、ゴール日が決まった状態
- **見積もり:** 1 セッション
- **整備する Claude Code 機能:** commands(`/ty-plan` `/ty-end` `/ty-gate`)

### P1 要件定義
- **目的:** 作るものと**作らないもの**を確定する
- **成果物:** `10_requirements/requirements.md`(FR-xx / NFR-xx) / `use-cases.md` / `glossary.md`
- **完了条件:** MVP 全機能に FR-ID が振られ、スコープ外が明記された状態
- **見積もり:** 2〜3 セッション
- **ゲート:** ① `doc-reviewer`
- **整備する Claude Code 機能:** agents(レビュアー)

### P2 基本設計(外部設計)
- **目的:** 画面・API・テーブルを確定し、詳細設計に着手できる状態にする
- **成果物:** `20_basic-design/system-architecture.md` / `screen-design.md` / `api-spec.yaml` / `er-diagram.md` / `table-definition.md` / `nonfunctional-design.md`
- **完了条件:** 画面・API・テーブルの3点が相互整合し、各項目が FR-ID までトレースできる状態
- **見積もり:** 3〜4 セッション
- **ゲート:** ② `doc-reviewer` / `test-reviewer` / `ops-reviewer`
- **整備する Claude Code 機能:** skills

### P2.5 アーキテクチャ検証(Walking Skeleton)
- **目的:** 中身が空のアプリを先に本番へ出し、デプロイ未達リスクを潰す
- **成果物:** 動く空アプリ / **公開 URL** / `20_basic-design/deployment.md`
- **完了条件:** 本番 URL 上で Vue → Java → PostgreSQL を経由した文字列が1つ表示される状態
- **見積もり:** 1〜2 セッション
- **整備する Claude Code 機能:** settings.json の permissions

### P3 詳細設計(内部設計)
- **目的:** 実装者が迷わない粒度までロジックを書き下す
- **成果物:** `30_detail-design/class-design.md` / `sequence.md` / `logic-spec/romaji-automaton.md` / `db-access.md`
- **完了条件:** 設計書だけを渡した Claude が実装を書き切れる粒度になっている状態
- **見積もり:** 2〜3 セッション
- **ゲート:** ③ 3観点
- **整備する Claude Code 機能:** rules(`paths:` スコープ)

### P4 テスト設計
- **目的:** 実装前にテストケースを固め、設計の抜けを炙り出す
- **成果物:** `40_test/test-plan.md` / `ut-cases.md` / `it-cases.md` / `st-cases.md` / `traceability-matrix.md`
- **完了条件:** 全 FR-ID にテストケース ID が1件以上紐づいた状態
- **見積もり:** 1〜2 セッション
- **整備する Claude Code 機能:** skills 追加(`testcase-gen`)

### P5 実装
- **目的:** 設計書どおりに作る
- **順序:** ロジックコア(TDD)→ DB/リポジトリ → API → フロント
- **完了条件:** ローカルで MVP の全 FR が動く状態
- **見積もり:** 6〜10 セッション
- **整備する Claude Code 機能:** hooks(PostToolUse)
- **縮小判断:** 8 セッションを超えたら履歴画面を切って P6 へ進む(デプロイ到達を最優先)

### P6 テスト実施
- **目的:** 設計どおりに動くことを証明する
- **成果物:** `40_test/test-results.md` / `bug-list.md`
- **完了条件:** ST 全件 PASS、未解決バグ 0(または残課題として明記)
- **見積もり:** 2〜3 セッション

### P7 リリース
- **目的:** 他人に渡せる状態にする
- **成果物:** `50_release/release-note.md` / `operation.md` / `README.md`
- **完了条件:** 公開 URL を他人にそのまま渡せる状態
- **見積もり:** 1〜2 セッション
- **整備する Claude Code 機能:** PreToolUse hook(破壊的コマンドのガード)

### P8 振り返り
- **目的:** 見積もり精度の補正係数を出す
- **成果物:** `00_project/retrospective.md` / Obsidian `30_Literature/` へ投下
- **完了条件:** 工程別ズレ率が数値化され「次回の補正係数は N 倍」が1行で書かれた状態
- **見積もり:** 1 セッション

### P9 (任意)多言語移植
- **目的:** ロジック仕様が本当に言語非依存だったかを検証する
- **完了条件:** ローマ字オートマトンの Java 版(`backend/typing-core`)を、実働版である TypeScript 版から移植し、共有テストベクタで同じ結果を返す状態(ADR-002: P5 時点の実働実装はフロントエンドの TypeScript のみのため、移植の向きは TS → Java)
- **見積もり:** 2〜3 セッション

---

## コマンド一覧

| コマンド | タイミング | やること |
|---|---|---|
| `/ty-plan` | セッション開始時 | 現在地確認 → 前回の打ち手の確認 → **Kazuki が先に見積もり** → 合意値を記録 |
| `/ty-end` | セッション終了時 | 実績記録 → ズレ率計算 → 要因分析 → 次回の打ち手 |
| `/ty-gate <工程ID>` | 工程の最終回 | 完了条件と成果物を照合し、未達を列挙 |

以下は各工程で必要になった時点で追加する。

| コマンド | 追加時期 | やること |
|---|---|---|
| `/ty-review <doc>` | P1 | レビュアーエージェントを起動し指摘票を生成 |
| `/ty-trace` | P4 | FR-ID ↔ 設計 ↔ テストケースの抜けを検出 |
| `/ty-bug` | P6 | バグ起票(ID 採番・再現手順・ステータス) |

---

## レビューの進め方

1人開発でレビュアーが居ない問題は、**独立コンテキストのサブエージェント**で解く。同じ会話にいる Claude は自分が書いた設計を肯定しやすいため、成果物ファイルだけを渡した別エージェントに読ませる。

- 指摘は `docs/90_review/review-YYYYMMDD_<doc>.md` に記録する
- 重要度は **A(必須) / B(推奨) / C(参考)** の3段階
- **ゲート通過条件: 重要度 A の指摘が全件クローズ**
