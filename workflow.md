# typing-app — 開発工程フロー

## 全体像

```
P0  プロジェクト計画   ──┐
P1  要件定義          ──┤→ ゲート① doc-reviewer
P2  基本設計          ──┤→ ゲート② 3観点レビュー
P2.5 アーキ検証(デプロイ前倒し)
P3  詳細設計          ──┤→ ゲート③ 3観点レビュー
P4  テスト設計
P5  実装
P6  テスト実施
P7  リリース
P8  振り返り
P9  (任意)多言語移植
```

**前工程のゲートが通るまで次工程に進まない。** ゲート通過条件は「重要度 A の指摘が全件クローズ」。

---

## 1セッションの流れ

```
┌──────────────────────────────────────────┐
│  1. 開始    /ty-plan   (15分)             │
│     ↓  ※見積もりは Kazuki が先に言う      │
│  2. 作業    (2〜2.5時間)                  │
│     ↓                                     │
│  3. ゲート  /ty-gate   (工程末のみ・15分)  │
│     ↓                                     │
│  4. 終了    /ty-end    (15分)             │
└──────────────────────────────────────────┘
```

### 1. 開始 `/ty-plan`
**目的:** 現在地を確認し、今日やることと見積もりを決める

- `progress.md` と `docs/00_project/wbs.md` を読んで現在地を提示
- **前回の「次回の打ち手」を読み上げる**(実行されなければ振り返りではない)
- 今日のタスクを決め、**Kazuki が先に見積もりを言う** → Claude が独立見積もり → 差分の根拠を突き合わせ → 合意値を記録

**アウトプット:** 今日のタスクと合意見積もりが `estimate-actual.md` に記録された状態

### 2. 作業
工程ごとの成果物を作る。詳細は下の工程一覧。

### 3. ゲート `/ty-gate`(工程の最後のセッションのみ)
**目的:** 次工程に進んでよいかを判定する

- 工程の完了条件と成果物の存在を照合し、未達を列挙
- レビュアーエージェントを起動し、指摘票を `docs/90_review/` に作成
- 重要度 A の指摘が残っていれば次工程に進まない

**アウトプット:** ゲート判定結果と指摘票

### 4. 終了 `/ty-end`
**目的:** 実績を記録し、ズレの原因を言語化する

- 実績時間を記録し、ズレ率を計算
- ズレの**原因**と**次回の打ち手**を対話で掘る
- `progress.md` を更新

**アウトプット:** `estimate-actual.md` に実績行が追加され、`progress.md` が更新された状態

---

## 工程一覧

| # | 工程 | 主な成果物 | 完了条件 | 見積(セッション) |
|---|---|---|---|---|
| P0 | プロジェクト計画 | `charter.md` / `wbs.md` / `estimate-actual.md` / `progress.md` / `workflow.md` | 全工程の WBS とタスク別見積り時間が1枚にあり、ゴール日が決まった状態 | 1 |
| P1 | 要件定義 | `10_requirements/requirements.md` / `use-cases.md` / `glossary.md` | MVP 全機能に FR-ID が振られ、スコープ外が明記された状態 | 2〜3 |
| P2 | 基本設計 | `20_basic-design/` 配下(システム構成 / 画面 / API / ER / 非機能) | 画面・API・テーブルが相互整合し、各項目が FR-ID までトレースできる状態 | 3〜4 |
| P2.5 | アーキ検証 | 動く空アプリ / **公開 URL** / `deployment.md` | 本番 URL 上で Vue → Java → PostgreSQL を経由した文字列が1つ表示される状態 | 1〜2 |
| P3 | 詳細設計 | `30_detail-design/` 配下(クラス / シーケンス / ロジック仕様 / DB アクセス) | 設計書だけを渡した Claude が実装を書き切れる粒度になっている状態 | 2〜3 |
| P4 | テスト設計 | `40_test/` 配下(計画 / UT / IT / ST / トレーサビリティ) | 全 FR-ID にテストケース ID が1件以上紐づいた状態 | 1〜2 |
| P5 | 実装 | `backend/` / `frontend/` | ローカルで MVP の全 FR が動く状態 | 6〜10 |
| P6 | テスト実施 | `test-results.md` / `bug-list.md` | ST 全件 PASS、未解決バグ 0(または残課題として明記) | 2〜3 |
| P7 | リリース | `50_release/` 配下 / `README.md` | 公開 URL を他人にそのまま渡せる状態 | 1〜2 |
| P8 | 振り返り | `retrospective.md` / Vault `30_Literature/` へ投下 | 工程別ズレ率が数値化され「次回の補正係数は N 倍」が1行で書かれた状態 | 1 |
| P9 | (任意)多言語移植 | TypeScript 版ロジックコア | 共有テストベクタで Java 版と同値であることが検証された状態 | 2〜3 |

**縮小の判断ポイント:** P5 が 8 セッションを超えたら履歴画面を切って P6 へ進む。デプロイ到達を最優先する。

---

## コマンド一覧

| コマンド | タイミング | やること | 導入工程 |
|---|---|---|---|
| `/ty-plan` | セッション開始時 | 現在地確認 → 今日のタスク決め → 見積もり | P0 |
| `/ty-end` | セッション終了時 | 実績記録 → ズレ分析 → 進捗更新 | P0 |
| `/ty-gate` | 工程の最後 | ゲート判定 + レビュー起動 | P0 |
| `/ty-review <doc>` | 成果物ができたとき | レビュアーエージェント起動 → 指摘票生成 | P1 |
| `/ty-trace` | P4 以降 | FR-ID ↔ 設計 ↔ テストのトレース抜け検出 | P4 |
| `/ty-bug` | P6 | バグ起票(ID 採番・再現手順・ステータス) | P6 |

> コマンドは `typing-app/` をルートとして Claude Code を起動した状態で使う。

---

## Claude Code 機能の整備計画

工程で実際に困ったことだけを解決する形で導入する。

| 工程 | 導入する機能 | 動機 |
|---|---|---|
| P0 | `CLAUDE.md` + commands 3本 | 進め方そのものを固定する |
| P1 | `agents/` | 1人開発でレビュアーが居ない問題が実際に起きる |
| P2 | `skills/` | 設計書生成は判断込みの手順で、command の直線手順では書けない |
| P2.5 | `settings.json` の permissions | ビルド・デプロイで承認プロンプトが鬱陶しくなる |
| P3 | `rules/`(`paths:` スコープ) | ファイル位置ごとに違う制約が生まれる |
| P4 | skill 追加 | テストケース生成 + トレーサビリティ検証 |
| P5 | `hooks/`(PostToolUse) | 「毎回・確実に」が要る決定論的処理が発生する |
| P7 | PreToolUse hook | 本番向け破壊的コマンドのガード |
| P8 | Vault 連携 | 既存 `insight-harvest` スキルへの接続 |

---

## 時間の目安

| 状況 | 推奨時間 |
|---|---|
| 通常セッション | 2.5〜3時間(開始15分 + 作業2〜2.5時間 + 終了15分) |
| 工程末セッション | 3時間(上記 + ゲート15分) |
| 隙間時間 | 30分(ドキュメントの見直し、レビュー指摘の対応のみ) |
