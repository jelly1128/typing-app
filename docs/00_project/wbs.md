---
doc_id: PRJ-002
status: draft
updated: 2026-09-07
---

# WBS(作業分解構成図)と見積もり

## 計画の方針: ローリングウェーブ

**近い工程は細かく、遠い工程は粗く**分解する。P2 以降のタスクを今から精密に割ろうとしても、要件が固まっていないので必ず外れる。各工程に入る直前に、その工程を時間単位まで分解する。

- **確定済み**: 現在の工程 + 次の工程 → タスク単位(2〜4時間)まで分解し、時間見積もりを持つ
- **暫定**: それ以降 → セッション数のみ

タスク単位の時間見積もりは `/ty-plan` で毎セッション取り、`estimate-actual.md` に記録する。

---

## 全体サマリー

| 工程 | セッション数 | 時間(h) | 状態 |
|---|---|---|---|
| P0 プロジェクト計画 | 1 | 2.5 | 進行中 |
| P1 要件定義 | 4 | 10.0 | 未着手(タスク分解済み) |
| P2 基本設計 | 4.5〜5 | 11.25 | 完了 |
| P2.5 アーキ検証 | 1〜2 | 2.5〜5 | 未着手 |
| P3 詳細設計 | 2〜3 | 10.5(確定) | 完了 |
| P4 テスト設計 | 1〜2 | 3.75(確定) | 完了 |
| P5 実装 | 6〜10(暫定、実質約18セッション見込み) | 15〜25(タスク積み上げ後45.0) | 進行中(タスク分解済み) |
| P6 テスト実施 | 2〜3 | 5〜7.5 | 未着手 |
| P7 リリース | 1〜2 | 2.5〜5 | 未着手 |
| P8 振り返り | 1 | 2.5 | 未着手 |
| **小計(MVP完走)** | **23.5〜33** | **58.75〜81.25** | |
| P9 多言語移植(任意) | 2〜3 | 5〜7.5 | 未着手 |

**ゴール予定日:** 休日 週1〜2セッション想定 → 23.5〜33 セッション ÷ 1.5 ≒ **15.7〜22 週 ≒ 2026年12月〜2027年1月**

> **注:** P1 の値はタスクからの積み上げ(実績ベースの根拠あり)。P2 以降はまだトップダウンの粗い値。各工程の直前に分解すると、P1 と同様に上振れる可能性が高い。P8 の補正係数はこのズレを定量化するためにある。

---

## P0 プロジェクト計画(確定)

| タスクID | 内容 | 完了条件 | 見積(h) | 実績(h) | 状態 |
|---|---|---|---|---|---|
| P0-01 | 計画策定・スコープ叩き台の合意 | 工程・規模・技術スタックが決まった状態 | 1.0 | 1.0 | 完了 |
| P0-02 | 開発環境の確認と不足ツールの導入 | Docker でコンテナが起動できる状態 | 0.5 | | 完了 |
| P0-03 | ディレクトリ骨格 + `CLAUDE.md` 作成 | 新セッションで CLAUDE.md が自動ロードされる状態 | 0.5 | | 完了 |
| P0-04 | `workflow.md` / `progress.md` / `docs/` 雛形作成 | 全工程の完了条件が1枚で読める状態 | 1.0 | | 完了 |
| P0-05 | commands 3本作成(`/ty-plan` `/ty-end` `/ty-gate`) | 3コマンドが実際に起動して動く状態 | 1.0 | | 完了 |
| P0-06 | WBS 作成 + P1 の見積もり | 全工程のセッション数とゴール日が決まった状態 | 0.5 | | 進行中 |
| P0-07 | 初回コミット | git log にコミットが1件ある状態 | 0.25 | | 完了 |

> **P0-02 の実績:** Docker Engine 29.7.2 / Docker Compose v5.4.0 が導入済みで、`hello-world` コンテナの pull → 実行まで確認済み。`psql` クライアントはホスト未導入だが、`docker compose exec` で代替できるため P2.5 まで保留する。

---

## P1 要件定義(確定)

| タスクID | 内容 | 完了条件 | Kazuki見積 | Claude見積 | 合意値 | 状態 |
|---|---|---|---|---|---|---|
| P1-01 | ユースケース洗い出し | 利用者が何をするかが一覧化された状態 | 1.0 | 1.0 | **1.0** | 完了 |
| P1-02 | 機能要件の定義(FR-ID 採番) | MVP 全機能に FR-ID が振られた状態 | 1.0 | 2.0 | **2.0** | 完了 |
| P1-03 | 非機能要件の定義(NFR-ID 採番) | 性能・可用性・セキュリティ・運用の各観点が埋まった状態 | 1.0 | 1.5 | **1.5** | 完了 |
| P1-04 | **スコープ外の明文化** | 「やらないこと」が理由付きで列挙された状態 | 1.0 | 0.75 | **1.0** | 完了 |
| P1-05 | 用語集の作成 | ローマ字判定まわりの用語が定義された状態 | 1.0 | 0.75 | **0.75** | 完了 |
| P1-06 | `doc-reviewer` エージェント作成 | 独立コンテキストでレビューが起動する状態 | 1.0 | 1.5 | **1.75** | 完了(次回セッション冒頭で Agent 一覧に表示されることを確認) |
| P1-07 | ゲート① レビュー実施と指摘対応 | 重要度 A の指摘が全件クローズした状態 | 1.0 | 1.0 | **1.0** | 完了(A=0件で通過。B3対応済み、B1/B2はP2引き継ぎ) |
| | **合計** | | **7.0 + バッファ3.0** | **9.5** | **10.0** | |

> P1-04 が要件定義で一番重要。Claude に「これも要るのでは」を挙げさせ、**意識的に切る**こと。切った理由が拡張バックログの根拠になる。

### 見積もり突き合わせの記録(2026-08-23)

**合計はほぼ一致(10.0 vs 9.5)。差が出たのは配分。** Kazuki は全タスクを一律 1.0h に置き、バッファを別枠 3.0h で持つ形だった。

一律配分 + 一括バッファの問題は精度ではなく、**バッファが誰のものか後から分からない**こと。P1-07 が 3h かかったとき、バッファを消費したのか見積もりが外れたのかを区別できず、ズレ率も補正係数も算出できない。→ **合計 10.0h は Kazuki の値を採用し、バッファを各タスクへ配分した。**

Claude が高く見た根拠:

| ID | 理由 |
|---|---|
| P1-02 | FR-01(ローマ字判定)の要件記述が難物。「し=si/shi/ci を許容」を実装に落とさず曖昧語も使わずに書く必要があり、必ず止まる |
| P1-07 | レビュー実行は5分。**本体は指摘対応**。初回の要件定義書には重要度 A が 5〜10件出る想定 |
| P1-06 | 新機能の初導入は1回で決まらない。書式調査 → 作成 → 起動 → 期待した指摘が出ずプロンプト修正、が入る |
| P1-03 | 観点を並べるのは速いが**値を決めるのが遅い**。「入力遅延 50ms 以下」の 50 の根拠づけ |

**ボトムアップがトップダウンを否定した:** 計画時の P1 は「2〜3セッション(5〜7.5h)」だったが、タスクから積むと 10.0h = **4セッション**。根拠のある積み上げ側を採用し、全体サマリーを修正した。

---

## P2 基本設計(確定)

| タスクID | 内容 | 完了条件 | Kazuki見積 | Claude見積 | 合意値 | 状態 |
|---|---|---|---|---|---|---|
| P2-01 | システム構成図作成(`system-architecture.md`) | Vue/Java/PostgreSQL の各層と責務が確定した状態 | 1.5 | 1.0 | **1.0** | 完了(ADR-002: リアルタイム判定はフロントエンド完結) |
| P2-02 | 画面設計(`screen-design.md`) | 画面遷移・入力/表示項目が FR-ID にトレースできる状態(B2: 自己ベスト・履歴0件時の挙動もここで確定) | 1.5 | 2.0 | **2.0** | 完了(B2対応済み) |
| P2-03 | ER図+テーブル定義(`er-diagram.md` / `table-definition.md`) | 全テーブルに TBL-ID が振られ、画面・API に必要なデータが揃っている状態 | 1.5 | 1.75 | **1.75** | 完了(ミス記録粒度の未決事項に回答。自由入力＋事後診断モードは拡張バックログへ) |
| P2-04 | API仕様(`api-spec.yaml`, OpenAPI) | 画面設計から必要なエンドポイントが洗い出され、FR-ID にトレースできる状態 | 1.5 | 2.0 | **2.0** | 完了(7エンドポイント、全FR-IDトレース済み) |
| P2-05 | 非機能設計(`nonfunctional-design.md`) | 全 NFR-ID に実現方式が書かれた状態(B1: リスク対策表への指標反映もここで対応) | 1.5 | 1.5 | **1.5** | 完了(B1対応済み、review-20260823_p1-gate1.mdにクローズ記録) |
| P2-06 | skills/レビュアーエージェント整備(test-reviewer / ops-reviewer 他) | ゲート②に必要なレビュアーが起動して動く状態 | 1.5 | 2.0 | **2.0** | 完了(P2-06a: doc-reviewer Opus再レビュー、P2-06b: test-reviewer/ops-reviewer作成) |
| P2-07 | ゲート② レビュー実施と指摘対応(doc/test/ops 3観点) | 重要度 A の指摘が全件クローズした状態 | 1.5 | 1.0 | **1.0** | 完了(A計6件中5件クローズ、ops A2はP2.5送りとして合意。ゲート②通過) |
| | **合計** | | **10.5** | **11.25** | **11.25** | |

依存順: P2-01 → P2-02 → P2-03 → P2-04 → P2-05 →(P2-06は並行可)→ P2-07。ER図とテーブル定義は導出関係が密なため1タスクにまとめた。

### 見積もり突き合わせの記録(2026-08-23)

Kazuki は全タスク一律 1.5h(P1のような内訳の根拠なし)。Claude は内容ベースの個別見積もり。差分1.5倍以上だったのは2件。

- **P2-01(システム構成図):** Claude 1.0h に対し Kazuki 1.5h。突き合わせの結果、Kazuki の値は「初めての作業に対する慎重な見積もり」であり、内容固有の根拠ではなかったため、Claude の値を採用
- **P2-07(ゲート②レビュー):** Claude 1.0h に対し Kazuki 1.5h。突き合わせの結果、Kazuki も「レビューは1hで済みそう」と同意したため、Claude の値を採用

残り5タスクは差分1.5倍未満だが、Kazuki の値が一律の直感値だったのに対し Claude の値は内容に基づく個別根拠を持つため、Claude 側を合意値として採用した。

**ボトムアップがトップダウンを再び上回った:** 計画時の P2 は「3〜4セッション(7.5〜10h)」だったが、積み上げると 11.25h ≒ 4.5〜5セッション。P1 と同じパターン(要件が細部まで見えるほど時間は増える)。

---

## P2.5 以降(暫定 — 各工程の直前に分解する)

### P2.5 アーキ検証(確定・2026-08-29分解)

**目的:** 設計書どおりに Vue → Java → PostgreSQL → Render の経路が本当に通るかだけを確認する Walking Skeleton。機能は作らない(雛形コードは P5 で作り直す前提)。

| タスクID | 内容 | 完了条件 | Kazuki見積 | Claude見積 | 合意値 | 状態 |
|---|---|---|---|---|---|---|
| P2.5-01 | Render 無料枠バックアップ/有効期限確認 → NFR-08確定 | `nonfunctional-design.md` NFR-08 が方針(a)/(b)/(c)のいずれかで確定した状態 | 0.67 | 0.5 | **0.5** | 完了(方針(c)受入リスク採用) |
| P2.5-02 | Docker Compose 雛形(PostgreSQL) | ローカルで `docker compose up` により PostgreSQL が起動する状態 | 0.67 | 0.5 | **0.5** | 完了(PostgreSQL 16.15 起動・psql接続確認済み) |
| P2.5-03 | Spring Boot 雛形(Hello World API) | `backend/` にビルドが通る雛形ができ、Hello World 相当のAPIが動く状態 | 0.67 | 1.25 | **1.25** | 完了(Java 25 + Spring Boot 4.1.1、`GET /api/hello`がJDBC経由でPostgreSQLから文字列取得・返却まで確認済み) |
| P2.5-04 | Vue 雛形(API 呼び出し画面) | `frontend/` の雛形が P2.5-03 のAPIを叩いて画面に表示する状態 | 0.5 | 0.5 | **0.5** | 完了(Vite proxy経由で`/api/hello`を取得し画面表示まで確認済み) |
| P2.5-05 | フロント/バックエンド配信方式の決定 | `deployment.md`(BD-007)が作成された状態 | 0.5 | 0.5 | **0.5** | 完了(Static Site + Web Service分離、Rewriteでプロキシ。CORS設定不要) |
| P2.5-06 | Render デプロイ・疎通確認 | 本番URL上で Vue→Java→PostgreSQL 経由の文字列が表示される状態(P2.5完了条件そのもの) | 0.5 | 1.75 | **1.75** | 完了(GitHub push→Docker化→Render 3サービス作成→Rewriteで疎通確認まで完了。フロント https://typing-app-1-jofu.onrender.com / バックエンド https://typing-app-xvkn.onrender.com) |
| P2.5-07 | settings.json permissions 整備 | Claude Code機能整備状況の該当欄が埋まる状態 | 0.5 | 0.25 | **0.25** | 完了(.claude/settings.jsonにgit/docker/mvn/npm/gh/java/node/curlの許可リストと破壊的操作のdenyを追加) |

### 見積もり突き合わせの記録(2026-08-29、続き)

P2.5-04〜07もKazuki一律0.5h(根拠なし、と本人が明言)。P2.5-06(1.75h、3.5倍差)とP2.5-07(0.25h、逆に0.5倍)はClaude値を採用。P2.5-06はGitHubリモート未設定・Renderアカウント未作成・フロント/バックエンド初回デプロイの3点が重なるため高め、P2.5-07は定型作業のため低めに見積もった。

### 見積もり突き合わせの記録(2026-08-29)

Kazuki は全タスク一律 0.67h(40分)の直感値(根拠なし、と本人が明言)。P2.5-03(Spring Boot雛形)のみ Claude 1.25h と1.87倍の差があったが、Kazuki側に個別の根拠がなかったため Claude の値を採用。根拠: このプロジェクトで初めて Java に触れるタスクであること、Java 25 + Spring Boot 4.x がまだ枯れていないバージョンであること、`CLAUDE.md` の制約(`typing-core` は言語非依存)を満たすマルチモジュール構成の決定が雛形作成に含まれること。

### P3 詳細設計(確定・2026-08-29分解)

**目的:** 実装者(Claude)が設計書だけを見て迷わず書き切れる粒度まで、ロジック・クラス・アクセス方式を確定する。

依存順: (P3-01〜03 ロジック仕様は並行可)→ P3-04・P3-05(クラス設計。ロジック仕様のI/Oに依存)→ P3-06(DBアクセス設計)→ P3-07(シーケンス図。クラス設計・DBアクセスに依存)→(P3-08 rules整備は並行可)→ P3-09(ゲート③)

| タスクID | 内容 | 完了条件 | Kazuki見積 | Claude見積 | 合意値 | 状態 |
|---|---|---|---|---|---|---|
| P3-01 | ロジック仕様: ローマ字オートマトン(`logic-spec/romaji-automaton.md`、FR-02) | かな1拍ごとの受理パターン(清音異表記/拗音/撥音「ん」/促音「っ」/長音)が言語非依存の疑似コードで全マッピング確定し、`shared/testdata` のテストベクタ設計に落とせる状態 | 1.5 | 3.0 | **3.0** | 完了(促音の独立表記(xtu/xtsu/ltu/ltsu)をKazuki指摘で追加。判定オートマトンに具体例セクション(6.1)を追記) |
| P3-02 | ロジック仕様: セッション集計計算(`logic-spec/session-metrics.md`、FR-06) | Net/Raw KPM・正確率・Consistency(標準偏差 or 変動係数)の算出式が確定し、打鍵ログ入力から結果オブジェクト出力までの手順が疑似コードで書ける状態 | 1.5 | 1.0 | **1.0** | 完了(Consistencyは標準偏差[ms]を採用。table-definition.mdの桁数未決事項も確認済み) |
| P3-03 | ロジック仕様: 改善アドバイス生成(`logic-spec/advice-generation.md`、FR-11) | FR-10の4観点の分析結果から、閾値判定→文言選択までのロジックが疑似コードで確定した状態 | 1.5 | 1.25 | **1.25** | 完了(Kazuki要望で単一選択→複数アドバイス方式に変更。api-spec.yaml/screen-design.mdの確定済み箇所をCL-005で修正) |
| P3-04 | クラス設計(バックエンド、`class-design.md`) | `api`(Controller/Service/Repository/Entity)と`typing-core`(集計ロジック)のクラス責務・パッケージ構成がFR-IDにトレースできる状態 | 1.5 | 0.75 | **0.75** | 完了(class-design.md 1章。FR-11をtyping-coreに含める判断をCL-009で記録、system-architecture.md修正) |
| P3-05 | クラス設計(フロントエンド、`class-design.md`) | Vueコンポーネント構成・ローマ字判定エンジン(TS)モジュール構成が確定し、FR-IDにトレースできる状態(system-architecture.md 4章の未決事項に対応) | 1.5 | 0.75 | **0.75** | 完了(class-design.md 2章。judgment-engine/api/stores/views/componentsのフォルダ構成確定。sessionStoreは記録専用でロジックを持たない方針) |
| P3-06 | DBアクセス設計(`db-access.md`) | 全テーブルへのJPAエンティティマッピング、FR-08/09/10の集計クエリ方針、NFR-09のトランザクション境界が確定した状態 | 1.5 | 0.5 | **0.5** | 完了(db-access.md作成。TBL-05 expected_key記録規則・インデックス設計の未決事項も解決、CL-010で記録) |
| P3-07 | シーケンス図(`sequence.md`) | セッション実行〜結果保存、履歴/自己ベスト/ミス分析取得、DB障害時(NFR-09)・利用者未存在時(ADR-003)のエラー系を含む主要フローの図が、クラス設計・DBアクセス設計と整合した状態 | 1.5 | 0.5 | **0.5** | 完了(sequence.md作成。正常系4本+エラー系2本のmermaidシーケンス図) |
| P3-08 | rules(`paths:`スコープ)整備 | `typing-core`の言語非依存制約など、パス単位のルールが実際に該当ディレクトリで読み込まれる状態 | 1.5 | 0.5 | **0.5** | 完了(`.claude/rules/typing-core.md` / `judgment-engine.md` 作成。P1-06と同じ理由で同一セッション内では読み込み確認不可。次回セッション冒頭で確認する) |
| P3-09 | ゲート③ レビュー実施と指摘対応(doc/test/ops 3観点) | 重要度Aの指摘が全件クローズした状態 | 1.5→0.25+1.5(2段階に分割、estimate-actual.md参照) | 1.0→0.25+2.0 | **0.25+1.5** | A指摘対応完了(計17件、重複除き15件、全件クローズ。修正直後の検証レビュー(REV-013)でA7件を追加検出、全件クローズ。B29+4件・C17+7件は申し送り、うち13件はA対応のついでに解消済み。下記「B/C指摘の対応フェーズ振り分け」参照)。Kazuki最終確認完了(2026-08-30)。**ゲート③通過、P3完了** |
| P3-10 | (計画外)P3-01〜03 中間レビュー対応 | doc-reviewerによる中間レビュー(REV-009)の重要度A(6件)が全件クローズした状態 | - | - | 計画外 | 完了(A1: 長音仕様の上流(requirements.md/glossary.md)修正+ADR-004。A2: 撥音んのn/nn押し戻しロジック欠陥を修正。A3: 促音の絞り込み結果が次拍へ伝播しない配線ミスを修正。A4: byCharType出現0件の0除算・誤アドバイスを修正(occurrenceCount追加、CL-007)。A5: 上流未決事項3箇所の解決済み化。A6: 総キー入力0時の正確率既定値を100→0に修正(自己ベスト汚染防止)。B6: な行(な/に/ぬ/ね/の)も"n"で始まるためA2と同種の衝突が残っていた点を修正(子音種別の行分類を廃止し、次拍の実際の先頭文字を見る方式に変更)。B/C残り(B1〜B5・B7〜B11・C1〜C6)はゲート③で対応予定) |
| | **合計** | | **13.5** | **10.5** | **10.5** | |

### B/C指摘の対応フェーズ振り分け(2026-08-29)

ゲート通過条件は重要度Aのみ(`workflow.md`)。B29件・C17件、計46件は当初「B/Cは申し送り」とだけ記録していたが、Kazukiから「各項目がどのフェーズで消える見込みか整理してほしい」との要望を受け、フェーズ別に割り振った。

**解消済み(A対応中に「ついで」で直したもの、計10件):** doc-B2(byKanaソートキー)/doc-B3(未着手誤記)/doc-B4(sequence.md層表現)/doc-B6(Entity↔DTO変換責務)/test-B4(enum揺れ、doc-A1と同一)/test-B9(500/404後の導線・S-03抜け)/ops-B1(find-or-create競合)/ops-B2(例外マッピング表)/ops-B3(404ログ)/ops-B4(baseURL環境変数化リスク)

**P4(テスト設計)着手時に確認:** テストケースを書く過程で「書けるか書けないか」が判定基準になる項目。
- test-B1(Service/Controllerのメソッドシグネチャ未定義。UTケースID採番の前提になるためP4冒頭で確定させる)
- test-B2(NFR-01/02の計測ポイント未定義。ST合否判定に必要)
- test-B3(NFR-09の障害注入手段未定義。IT手順に必要)
- test-C1(shared/testdata形式未定義。P4冒頭タスクとlogic-spec自身が明記済み)
- test-C3 / ops-C3(自己ベストが終了条件をまたぐ。テストケースに1件追加すれば足りる)
- ~~doc-B9 / ops-B8 / test-C4(byKanaのoccurrenceCount欠如によるゼロ除算リスク)~~ → 解決済み。2026-08-30 P4-03でUT-065として境界値ケースを書いた結果、未解決であることを確認。Kazukiと相談のうえ`byKana`に`occurrenceCount`を追加する方針(byCharTypeと同じ対応)で解決した(CL-016)

**P5(実装)着手時に確認:** 設計判断としては軽微/自明で、コードを書く瞬間に決めれば足りる項目。
- doc-B1(expectedKey複合値のbyErrorPattern集計・表示方針)
- doc-B5(system-architecture.md updated日付の反映漏れ。1行修正)
- doc-B7 / ops-B9(日本語enum定数名のリスク検討・エンコーディング前提の明記)
- doc-B8(お題ローテーションのロジック所在。「配列を先頭から使い末尾で戻る」程度の自明な実装)
- test-B5(expectedKeyの重複除去・単一候補時の書式)
- test-B6(UserServiceのname境界値、trim/文字数カウント方式)
- test-B7(FR-10のRepositoryテストはPostgreSQL実体で行う旨の明記)
- test-B8(シードデータのsort_orderタイブレーク・最小要件)
- test-B10(SessionResult組み立て責務・playedAtの採番元)
- ops-B5(Flyway/JPA Entityのddl-auto固定)
- ops-B6(ヘルスチェックエンドポイント追加)
- ops-B10(FR-06縮小判断の実装方式。**縮小判断が実際に発動した場合のみ**対応する条件付き項目)
- doc-C2/C4/C5/C6/C9(節番号・表の並び順・別名の注記など、実装時に気づいた範囲で直せば足りる軽微な記述の話)

**P6(テスト実施)〜P8(振り返り)で実測してから判断:** 個人開発規模では当面問題にならないが、実測して悪化していれば対応するタイプ。
- ops-B7(履歴一覧・ミス分析クエリの件数上限。P6でデータ量を振って実測)
- test-C2/ops-C2(FR-10の全期間集計コスト。同上)

**P7(リリース)着手時に確認:**
- ops-C4(Renderのログ保持期間・DB接続タイムアウトの前提を`operation.md`に記録)

**対象外(次にP1文書を触る機会に反映):**
- doc-C8(`requirements.md` NFR-08の記述が古い。P3の対象文書ではないため今回は見送り)

**追記(2026-08-29、修正後の検証レビューREV-013より):** A7件を修正し、うちB3・B6・B7は修正のついでに解消済み。残りのB4件(verify-B1/B2/B4/B5)・C7件(verify-C1〜C7)はP5着手時の確認に追加する(いずれも実装の瞬間に決めれば足りる軽微な項目のため)。

### 見積もり突き合わせの記録(2026-08-29、続き)

`/ty-plan` でP3-04・P3-05に着手する回、前回セッション末の打ち手(見積もり前に「誰が実際に手を動かすか」を自問する)を適用した結果、当初合意値1.5h→0.75hに改訂した。理由はP3-01〜03と同型(Claudeがクラス一覧・責務・パッケージ構成をドラフトし、Kazukiは妥当性判断のみ)で、直近3件(P3-01〜03)の実績ズレ率平均-46.7%を根拠に採用。Kazuki側も「ドラフトはClaude側で作るのでClaudeの見積もりに合わせる」で即合意(突き合わせの往復なし)。

### 見積もり突き合わせの記録(2026-08-29)

Kazuki は「結構考える必要ありそう」という理由で全タスク一律1.5h(個別根拠なし)。Claudeは内容ベースの個別見積もり。合計はほぼ一致(13.5 vs 12.0)したが、1.5倍以上の差は2件。

- **P3-01(ローマ字オートマトン仕様):** Claude 3.0h に対し Kazuki 1.5h(2.0倍)。9タスクの中で唯一「全かな×全表記ゆれパターンの網羅」という突出して対象範囲が広いタスクであり、一律配分には反映されていなかった
- **P3-08(rules整備):** Claude 0.5h に対し Kazuki 1.5h(3.0倍、逆方向)。前回(P2.5-06/07)の教訓([progress.mdの次回の打ち手](../../progress.md)参照)がここでも再現し、「環境構築系」という理由だけで高く見積もっていた。実態はP2.5-07(settings.json整備)と同型の「Claudeが作成し、Kazukiは動作確認のみ」パターン

残りは1.5倍未満だが、Kazukiの値が一律の直感値だったのに対しClaudeの値は内容に基づく個別根拠を持つため、**全タスクでClaude値を合意値として採用**した(P2-01〜07と同じ扱い)。

### P4 テスト設計(確定・2026-08-30分解)

**目的:** 実装前にテストケースを固め、設計の抜けを炙り出す。

依存順: P4-01 →(P4-02は並行可)→ P4-03 → P4-04 → P4-05 → P4-06

| タスクID | 内容 | 完了条件 | Kazuki見積 | Claude見積 | 合意値 | 状態 |
|---|---|---|---|---|---|---|
| P4-00 | P4タスク分解 | 本表がタスク単位まで分解された状態 | 1.0(P4-01と合算) | 0.5 | **0.5** | 完了 |
| P4-01 | テスト計画(`test-plan.md`)作成 | テストレベル定義・UT/IT/ST命名規則(ID体系)・テスト環境方針が確定し、test-B1(メソッドシグネチャ未定義)/test-C1(`shared/testdata`形式未定義)が解決された状態 | 1.0(P4-00と合算) | 0.75 | **0.75** | 完了(Kazuki確認済み) |
| P4-02 | `testcase-gen` skill整備 | UT/IT/STケース作成を助けるskillが動く状態 | 1.0(P4-01〜06と合算) | 0.5 | **0.5** | 完了(`.claude/skills/testcase-gen/SKILL.md`作成。同一セッション内では動作確認不可、次回セッション冒頭で確認) |
| P4-03 | UTケース作成(`ut-cases.md`) | 全FR-IDのロジック層(typing-core)中心に単体テストケースが作成され、doc-B9/ops-B8/test-C4(byKanaのoccurrenceCountゼロ除算)の境界値ケースを含む状態 | 1.0(P4-01〜06と合算) | 0.75 | **0.75** | 完了(Kazuki確認済み。UT-005/UT-065追加、UT-065はCL-016で解決) |
| P4-04 | ITケース作成(`it-cases.md`) | API/DB結合テストケースが作成され、test-B3(NFR-09障害注入手段)が解決された状態 | 1.0(P4-01〜06と合算) | 0.5 | **0.5** | 完了(Kazuki確認済み) |
| P4-05 | STケース作成(`st-cases.md`) | シナリオベースの総合テストケースが作成され、test-B2(NFR-01/02計測ポイント)、test-C3/ops-C3(自己ベストが終了条件をまたぐケース)が解決された状態 | 1.0(P4-01〜06と合算) | 0.5 | **0.5** | 完了(Kazuki確認済み) |
| P4-06 | トレーサビリティマトリクス作成(`traceability-matrix.md`) | 全FR-IDにテストケースIDが1件以上紐づいた状態(P4完了条件そのもの) | 1.0(P4-01〜06と合算) | 0.25 | **0.25** | 完了(全FR-ID充足を確認。Kazuki確認済み。**P4完了**) |
| | **合計** | | -(P4-01が2つの合算見積もりに重複して含まれるため算出不可) | **3.75** | **3.75** | |

### 見積もり突き合わせの記録(2026-08-30)

`/ty-plan`でP4-00/P4-01に着手する回、Kazukiは「テストは未考慮の部分が浮き彫りになりそう」という理由でP4-00+P4-01を合算1hと見積もり(内訳なし)。対象が2タスクのみで規模も小さいため、Claudeの内容ベースの内訳(0.5h/0.75h、合計1.25h)をそのまま合意値として採用(即合意)。

続けてP4-01〜06(6タスク)の全体見積もりをKazukiに聞いたところ、こちらも合算1hだった。P4-01単体で既に0.75hと合意していたため、残り5タスク(skill整備+UT/IT/ST3ドキュメント+トレーサビリティ)に0.25h程度しか見込んでいない計算になり、3.25倍の差が出た。ClaudeはP3の同種ドキュメント作成タスク(db-access.md/sequence.mdが各0.5h)を参考に合計3.25hと見積もり、突き合わせを提案したが、Kazukiから個別の根拠は出ず「それでいい」とClaude値をそのまま採用した。**根拠の掘り下げが機能したかは実績を見てから判断する。**

### P5 実装(確定・2026-08-30分解)

**目的:** 設計書どおりに実装し、ローカルで MVP の全 FR が動く状態にする。

**方針(2026-08-29追記):** `typing-core`の実装はKazuki自身が手を動かして書く。Claudeはドラフトを書かず、仕様(`class-design.md`/`logic-spec/*.md`)通りかのレビューのみ行う。理由は「設計書をClaudeがドラフトしKazukiが判断するだけの進め方では実装力が身につかない」という懸念がP3セッションで出たため。

**追記(2026-08-30):** `shared/testdata`(P4で形式のみ決定、本体は1件も未作成)も、Claudeが下書きせずP5でKazuki自身がTDDのテストを書きながら埋める方針とした(手計算での期待値作成は誤りが混入しやすいため、テストを書きながら確定させる方が安全)。

**着手時相談の結果(2026-08-30):**
- `typing-core`と同じ理由(`class-design.md` 2.1「Vueに依存させないのはtyping-coreと同じ理由」)により、フロントの`judgment-engine`も同じ方針(Kazuki TDD、Claudeはレビューのみ)とする
- それ以外の層(Flyway/backend Controller・Service・Repository/frontend api・stores・views・router)はKazukiが主体で書き、詰まったらClaudeに相談してヒント・サンプルコードをもらう方針とする。初挑戦のトピックは詰まる前提で見積もる
- hooks(PostToolUse)整備は固定タスクとしない。CLAUDE.mdの学習目的(「その工程で実際に困ったことだけを解決する形で導入」)に従い、実装中に具体的な困りごとが出た時点で都度導入を検討する(workflow.md 102行目時点の記述は暫定であり、本追記で修正)
- E2Eフレームワーク(Playwright等)・Testcontainers導入は保留し、IT着手時に選定する

**方針転換(2026-08-30、同日中):** P5-01(`SessionMetricsCalculator`)に着手したところ、Maven依存関係の追加・JUnit5配線・Jacksonでのshared/testdata読み込みなど、**ロジック本体に到達する前の環境構築だけで大半の時間を消費**した。この部分はP3セッションの懸念(「設計書を読んで判断するだけでは実装力が身につかない」)が想定していた対象ではなく、Kazukiから「かけた時間に対して学習効果が見合わない」との申告があった。これを受け、**`typing-core`/`judgment-engine`もClaudeがドラフトし、Kazukiはレビュー・質問で理解を深める方針(P1〜P4と同じパターン)に変更**する。他の層(backend/frontend)は変更なし。P5-01は環境構築(pom.xml/record/shared testdata)まではKazukiが実施済みのため、残る`calculate()`本体のみClaudeがドラフトする。P5-02〜04は最初からClaudeドラフトで進める。

**再改訂(2026-09-07):** P5-05(Flyway migration)着手時にも同じ現象(Kazuki主体前提で見積もった後、着手直後にClaudeドラフトへ変更)が発生した。P5-01・P5-05の2例から「Kazuki主体・詰まったらClaude相談」という層区分自体がもう機能しないと判断し、**P5-06以降はbackend/frontend含む残り全タスクをClaudeドラフト・Kazukiレビュー方式に統一する**(層による使い分けは廃止)。

**注(2026-09-05):** 当初のP5-03完了条件は「`shared/testdata`の受理パターンが全て通る状態」だったが、`test-plan.md`3.1節の`romaji-automaton`(RA-xxx)形式は`sequenceJudge`(P5-04)のキー入力逐次呼び出しを前提にしており、P5-03単体(`moraPatterns`/`specialMora`/`moraJudge`)では検証できないと判明した。P5-03は各モジュールをVitestの通常ユニットテストで検証し、`shared/testdata/romaji-automaton/cases.json`(RA-xxx)の作成・全件Green確認はP5-04(`sequenceJudge`実装)で行う。

依存順: (P5-01〜04 typing-core/judgment-engineは並行可)→ P5-05(Flyway)→ P5-06(Entity/Repository)→ P5-07〜11(backend Controller/Service、並行可)→ P5-12(frontend api/types)→ P5-13(stores)→ P5-14〜17(views、並行可)→ P5-18(router)→ P5-19(結合確認、P5完了条件そのもの)

| タスクID | 内容 | 完了条件 | Kazuki見積 | Claude見積 | 合意値 | 状態 |
|---|---|---|---|---|---|---|
| P5-00 | P5タスク分解 | 本表がタスク単位まで分解された状態 | - | 0.5 | **0.5** | 完了 |
| P5-01 | typing-core: `SessionMetricsCalculator`実装(FR-06) | `shared/testdata`のテストベクタで全ケースが通る状態 | 3.0 | 2.0 | ~~3.0~~ **1.0**(2026-08-30方針転換、下記参照) | 完了(SM-001〜006全件Green) |
| P5-02 | typing-core: `AdviceGenerator`実装(FR-11) | `shared/testdata`のテストベクタで全ケースが通る状態 | 3.0 | 2.0 | ~~3.0~~ **1.0** | 完了(AdviceGenerator本体+周辺DTO+テスト7件全Green。救済コミットd367032) |
| P5-03 | judgment-engine: `types.ts`/`moraPatterns.ts`/`specialMora.ts`/`moraJudge.ts`実装(FR-02) | 各モジュールの単体テスト(Vitest)が全て通る状態(注参照) | 3.0 | 4.0 | ~~4.0~~ **2.0** | 完了(Vitest導入、4モジュール実装、30件Green。RA-xxx testdataはP5-04で作成) |
| P5-04 | judgment-engine: `sequenceJudge.ts`実装(FR-02〜04、状態保持含む全体) | お題文1本を最初から最後まで判定でき`KeystrokeResult`が仕様どおり返る状態 | 3.0 | 3.0 | ~~4.0~~ **1.5** | 完了(Vitest 14件+RA-xxx testdata 11件 全Green。着手時にCL-017/CL-018で設計書の矛盾2件を修正) |
| P5-05 | Flyway migration(TBL-01〜06 + シードデータ) | `docker compose up`で全テーブルが作成されシードデータが投入される状態 | ~~3.0~~ **1.5** | ~~1.5~~ **1.0** | ~~2.0~~ **1.0**(2026-09-06再見積。P5-01と同様、KazukiがFlywayを自分で書く前提からClaudeドラフト・Kazukiレビュー方式に変更したため) | 完了(V1__create_schema.sql全6テーブル+4インデックス、R__seed_topic_master.sqlで難易度3件・お題15文を投入。`docker compose up`→バックエンド起動でFlyway自動適用を確認済み) |
| P5-06 | Entity + Repository(6テーブル分) | 全EntityがJPAでマッピングされ、Repositoryのfind系メソッドが動く状態 | ~~3.0~~ **1.5** | ~~2.0~~ **1.0** | ~~3.0~~ **1.0**(2026-09-07再見積。P5-06以降Claudeドラフト・Kazukiレビュー方式に統一したため) | 完了(Entity 6種+Repository 6本+プロジェクション5種。RepositorySmokeTestで実DB接続確認、`ddl-auto=validate`でスキーマ整合性も検証) |
| P5-07 | `UserController`/`UserService`(FR-12) | find-or-createがAPI経由で動く状態 | 3.0 | 2.0 | **2.0** | 未着手 |
| P5-08 | `TopicSetController`/`TopicSetService`(FR-01, FR-13) | お題セット一覧・お題文一覧がAPI経由で取得できる状態 | 3.0 | 1.5 | **2.0** | 未着手 |
| P5-09 | `SessionController`/`SessionService`(FR-04〜09) | セッション結果の保存(トランザクション・自己ベスト比較含む)と履歴取得がAPI経由で動く状態 | 3.0 | 3.5 | **4.0** | 未着手 |
| P5-10 | `MissAnalysisController`/`MissAnalysisService`(FR-10, FR-11) | ミス分析4観点の集計とアドバイス生成がAPI経由で動く状態 | 3.0 | 2.5 | **3.0** | 未着手 |
| P5-11 | `GlobalExceptionHandler` + 例外クラス群 | class-design.md 1.4の例外→応答対応表どおりに4xx/500が返る状態 | 3.0 | 1.5 | **2.0** | 完了(`ThrowingTestController`+`@WebMvcTest`で対応表8行→5件のGreen確認。P5-07〜10の他Controllerからも同じ`GlobalExceptionHandler`を使う) |
| P5-12 | frontend `api/client.ts` + 各apiモジュール + `types/api.ts` | 全APIエンドポイントがフロントから型付きで呼べる状態 | 3.0 | 2.0 | **2.0** | 未着手 |
| P5-13 | `stores`(`userStore`/`topicStore`/`sessionStore`) | セッション中の集計・終了条件判定・送信失敗時再送がストア経由で動く状態 | 3.0 | 2.5 | **3.0** | 未着手 |
| P5-14 | views: `NameInputView`/`HomeView`(S-01, S-02) | 名前入力→ホーム画面遷移が動く状態 | 3.0 | 1.5 | **2.0** | 未着手 |
| P5-15 | views: `TypingView` + `TypingDisplay`(S-03) | judgment-engineの結果を使い実際にタイピングできる状態 | 3.0 | 3.0 | **4.0** | 未着手 |
| P5-16 | views: `ResultView` + `SessionMetricsSummary`(S-04) | セッション結果と自己ベスト比較が表示される状態 | 3.0 | 1.5 | **2.0** | 未着手 |
| P5-17 | views: `HistoryView`/`MissAnalysisView`/`MissAnalysisSection`(S-05, S-06) | 履歴一覧とミス分析4観点が表示される状態 | 3.0 | 2.0 | **3.0** | 未着手 |
| P5-18 | `router`整備(遷移ガード含む) | S-01〜S-06の遷移とuserId未設定時のガードが動く状態 | 3.0 | 1.0 | **2.0** | 未着手 |
| P5-19 | 結合確認 | ローカルでMVPの全FRが動く状態(P5完了条件そのもの) | 3.0 | 2.0 | **3.0** | 未着手 |
| | **合計** | | **57.0(P5-00除く)** | **41.0** | ~~53.5~~ **45.0**(2026-08-30 P5-01〜04再改訂後) | |

### 見積もり突き合わせの記録(2026-08-30)

Kazukiは全タスク一律3.0h(「ひとつずつ自分で作っていく想定なので実際3hぐらいかかるかも」)。Claudeは内容ベースの個別見積もり、合計41.0h。全体では1.39倍で従来の閾値(1.5倍)未満だが、**個別タスクでは最大3倍の差が出るものが混在**していた。

過去(P1〜P4)は「Claudeが手を動かす」前提だったため個別根拠のあるClaude値を合意値として採用してきたが、**P5は逆にKazuki自身が手を動かすタスクが大半**であり、Claudeの値は「他人(初挑戦の学習者)の速度を外から推測した数字」に過ぎない。そのためKazuki値をそのまま採用するのでも、Claude値で上書きするのでもなく、**複雑度3段階(複雑=4.0h/標準=3.0h/定型=2.0h)**で再配分した。

- 複雑(P5-03/04/09/15): judgment-engineの状態保持・SessionControllerのトランザクション/自己ベスト比較・TypingViewの結合。ゲート③の設計レビューで欠陥が集中した箇所と重なるため、Claude単独見積もりより高めに設定
- 標準(P5-01/02/06/10/13/17/19): Kazukiの一律3.0hをそのまま採用
- 定型(P5-05/07/08/11/12/14/16/18): 疑似コード/表をそのままなぞる機械的な実装が中心。Claude見積もりに近い2.0hに設定したが、初挑戦の技術スタックであることを考慮しKazuki値からは下げすぎない

**全体計画への影響:** 合意値合計53.5h(P5-00除く53.0h)は元のWBS想定(15〜25h、6〜10セッション)を大きく超える。2.5h/セッション換算で約21セッション相当となり、workflow.mdの「8セッション超過で履歴画面を切ってP6へ」という縮小判断が発動する可能性が高い。ゴール予定日への影響はP8での実績確認後に再計算する。

### 見積もり再改訂(2026-08-30、同日中の方針転換)

P5-01着手直後、環境構築の負荷とKazukiの申告により`typing-core`/`judgment-engine`をClaudeドラフトへ変更(上記「方針転換」参照)。これに伴いP5-01〜04を「Claudeドラフト・Kazuki意思決定のみ」パターンとして再見積もりした。根拠は`estimate-actual.md`「補正係数の推移」にある既存所見(このパターンは合意値を通常の0.3〜0.5倍程度に補正するとズレ率0%に近づく実績が複数回出ている)。

| タスクID | 変更前(手を動かす主体=Kazuki) | 変更後(主体=Claude) | 倍率 |
|---|---|---|---|
| P5-01 | 3.0h | 1.0h | 0.33倍 |
| P5-02 | 3.0h | 1.0h | 0.33倍 |
| P5-03 | 4.0h | 2.0h | 0.5倍(パターン自体はP3-01ローマ字仕様と同型で複雑度が高いため、他3件より高めに据え置き) |
| P5-04 | 4.0h | 1.5h | 0.375倍 |
| **小計** | **14.0h** | **5.5h** | |

P5-01のみ、環境構築(pom.xml/record定義/shared testdata作成)は既にKazukiが実施済みのため、合意値1.0hは「残りの`calculate()`本体の実装+動作確認」分。P5-02〜04はゼロからClaudeドラフトのため、合意値がそのままタスク全体にかかる想定時間。

**縮小判断: 8 セッション超過で履歴画面を切って P6 へ**

### P6 テスト実施 — 2〜3 セッション
UT / IT / ST 実施 / バグ起票・修正・再テスト

### P7 リリース — 1〜2 セッション
本番デプロイ / リリースノート / 運用手順 / README / PreToolUse hook

### P8 振り返り — 1 セッション
ズレ率集計 / 補正係数算出 / Vault へ投下
