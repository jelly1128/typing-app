---
doc_id: PRJ-004
status: fixed
updated: 2026-09-12
---

# 変更管理票

工程を先に進めた後で前工程の成果物を直した記録。**設計とコードが乖離したまま進まないための仕組み。**

## 運用ルール

- 実装中に設計との差異を見つけたら、**コードを書く前に設計書を直し**、その事実をここに1行残す
- 「なぜ設計時に気づけなかったか」を書く。これが次のプロジェクトの設計観点になる
- 却下した案そのものは ADR(`decisions/`)に書く。ここは**確定済みの成果物を変更した記録**に限る

## 記録

| ID | 日付 | 対象文書 | 変更内容 | 理由 | 設計時に気づけなかった原因 |
|---|---|---|---|---|---|
| CL-001 | 2026-08-23 | `workflow.md`(P9) | 「TypeScript 版が共有テストベクタを Java 版と同じ結果で通す」→「Java 版を TypeScript 版から移植し、共有テストベクタで同じ結果を返す」に移植の向きを修正 | P2-01(ADR-002)で、NFR-01(50ms)を満たすためリアルタイム判定はフロントエンド(TS)完結と決定した。結果、ローマ字オートマトンの実働実装は P5 時点で TS のみになり、Java(`typing-core`)側は当初から存在しない。P9 で Java 版を作る場合、移植元は TS になる | P0 時点では NFR-01 の具体的な遅延目標(50ms)も、判定をどのレイヤで行うかも未確定だった。「typing-core は Java」という技術スタックの取り決めから、素朴に「Java が原本、TS が移植先」と想定してしまい、リアルタイム制約から生じる実行場所の制約を考慮していなかった |
| CL-002 | 2026-08-23 | `charter.md`(6章 リスク表) | 「userId 推測による他人データ閲覧」リスクと受入判断(ADR-003)を追記 | P2-06a(doc-reviewer Opus版の再レビュー)の過程で、`/api/users/{userId}/...` 系エンドポイントに認可制御がなく、userId を知る第三者が他人の履歴・スコアを閲覧できる点が判明した | ADR-001(認証スコープ外)の検討時は「認証機構を作るか」だけを論点にしており、「認証なしで userId ベースAPIを公開した場合の閲覧リスクそのもの」は独立した論点として扱っていなかった |
| CL-003 | 2026-08-23 | `requirements.md` FR-04/FR-06 未決事項、`use-cases.md` 4章未決事項、`charter.md` 3.3 規模感 | FR-04・use-cases.md の保存粒度未決事項を解決済みに更新。FR-06 の正確率定義の未決事項を解決済みに更新。charter.md 3.3 をテーブル4〜5→6、API 6〜8→7本、画面4〜5→6に確定値へ更新 | P2-06a(REV-005、B4)で、P2 側の設計書(table-definition.md・api-spec.yaml)が既に回答済みの未決事項を、上流のP1文書側が未更新のまま放置していたことが判明した | P2-03〜P2-04 で個々の未決事項に回答した際、その回答を発生元のP1文書へ遡って反映する手順がなく、下流(P2)の更新だけで完結したと思い込んでいた |
| CL-004 | 2026-08-26 | `api-spec.yaml`(400/404/ErrorResponse追加、isNewBest→isNetKpmBest/isAccuracyBest分割、expectedKey説明追記)、`table-definition.md`(expected_key未決事項追記)、`screen-design.md`(userId 404時のlocalStorageクリア導線、「別の名前で始める」リンク、自己ベスト強調表示の対応先明記)、`nonfunctional-design.md`(NFR-07にログ項目・例外ハンドラ集約先を追記、NFR-08をP2.5確認待ちに改訂、NFR-09を400/404/500の切り分けに改訂)、`system-architecture.md`(`@RestControllerAdvice`集約の1行追記) | P2-07(ゲート②、test-reviewer/ops-reviewer初回実行、REV-007/REV-008)で検出した重要度A計6件(4xx未定義・isNewBest判定基準・expectedKey記録規則・エラーボディ/ログ項目・userId復旧導線)を解消した。バックアップ有無の確認(REV-008 A2)のみP2.5着手時の確認事項として`wbs.md`に申し送り | P2-04でAPI仕様を書いた時点では正常系(200/201)とDB障害時(500)の2系統しか想定しておらず、「利用者の入力ミス」「参照先が存在しない」「複数正解候補がある入力の記録規則」を別カテゴリとして洗い出す観点が抜けていた。test-reviewer/ops-reviewerという専用の観点を持つレビュアーを分離して初めて拾えた |
| CL-005 | 2026-08-29 | `api-spec.yaml`(`MissAnalysis.advice`を単一文字列/nullable→文字列配列に変更、エンドポイント説明も空配列ベースに修正)、`screen-design.md`(S-06の表示項目・4章B2の空状態表示を配列前提に修正) | P3-03(`logic-spec/advice-generation.md`)で改善アドバイスの生成方式を設計中、Kazukiから「複数の弱点があるなら全部教えてほしい」との要望が出た。優先順位1位のみを返す単一選択方式から、3カテゴリ(誤りパターン/かな別/文字種別)を独立判定し該当するもの全て(0〜3件)を返す方式に変更した | P1(FR-11)・P2(api-spec.yaml)の時点では「アドバイス」を単数の助言文としか想定しておらず、複数の弱点を同時に提示するニーズを検討していなかった。P3で生成ロジックの中身(閾値判定)を具体化して初めて「1件に絞る根拠」の弱さが表面化した |
| CL-006 | 2026-08-29 | `requirements.md`(FR-02受け入れ条件5)、`glossary.md`(長音「ー」の定義) | P3-01(`logic-spec/romaji-automaton.md`)で長音の受理表を疑似コード化する過程で、「直前の母音を延長する」方式(P1確定)は直前拍の表記ゆれと組み合わさって受理パターンが爆発することが判明し、"-"キー1つで確定する方式に変更した(ADR-004)。P3中間レビュー(REV-009 A1)で「上流を直さず下流だけ変更していた」ことを指摘され、本記録とあわせて上流2文書を修正した | P1で受け入れ条件を書いた時点では、長音の延長対象が「直前拍の複数表記のどれか」という組み合わせ問題になることを想定しておらず、字面どおり「延長すればよい」とだけ書いていた。ローマ字受理表を実際に疑似コードへ落とすP3まで組み合わせ爆発が顕在化しなかった |
| CL-007 | 2026-08-29 | `api-spec.yaml`(`MissAnalysis.byCharType`に`occurrenceCount`を追加、出現0件の文字種は配列に含めない旨を明記)、`screen-design.md`(S-06備考に出現0件文字種の非表示を追記) | P3中間レビュー(REV-009 A4)で、出現回数0の文字種(例: 初級お題に促音「っ」が無い)で正解率が0除算になり、かつ`advice-generation.md`のカテゴリ3判定が「一度も出題されていない文字種」に誤ってアドバイスを出す不具合が指摘された。`occurrenceCount`を追加し、0件は配列から除外する方式に修正した | P2-04でAPI仕様を書いた時点では「文字種5種類は必ずセッション中に出現する」ことを暗黙の前提にしており、難易度(お題セット)によって特定の文字種が一度も出現しないケースを想定していなかった |
| CL-008 | 2026-08-29 | `requirements.md`(FR-06/FR-11の未決事項、5章未決事項リスト)、`table-definition.md`(TBL-04 consistency桁数の未決事項) | P3-02/P3-03で回答済みだった未決事項が、発生元のP1/P2文書側で「未決」のまま更新されていなかった(CL-003と同型の再発)。P3中間レビュー(REV-009 A5)の指摘を受け、該当箇所を解決済みに更新した | CL-003で「下流の回答を上流に戻す手順がない」ことを一度指摘・記録していたが、P3-02/03を書いた際に同じ手順の抜けが再発した。個々の文書執筆時のチェックリストではなく、レビュー(doc-reviewer)による機械的な検出に頼る運用になっている |
| CL-009 | 2026-08-29 | `system-architecture.md`(3章FR対応表、typing-core行にFR-11を追加) | P3-04(クラス設計)で、FR-11(改善アドバイス生成)を`typing-core`に含めることにした。`logic-spec/advice-generation.md`がFR-06(`session-metrics.md`)と同じ「言語非依存の疑似コード」として書かれており、`shared/testdata`の検証対象にもなる以上、モジュール配置もFR-06と揃えるのが一貫している | P2-01時点(system-architecture.md初版)ではFR-11のアドバイス生成はまだ単一選択の簡易ロジックとして想定されており、疑似コード仕様として独立させる(P3-03)ほどの複雑さを持つとは見込んでいなかったため、typing-core行への計上が漏れていた |
| CL-010 | 2026-08-29 | `table-definition.md`(TBL-05 expected_key未決事項、インデックス設計未決事項)、`logic-spec/romaji-automaton.md`(7章の申し送り事項) | P3-06(`db-access.md`)で、両文書が「P3-06で確定する」と明記していたTBL-05 `expected_key`の記録規則(カンマ区切り・アルファベット順の文字列)とインデックス設計を確定し、両文書の該当箇所を解決済みに更新した | 差異の修正ではなく、当初から計画どおりP3-06で確定した未決事項。CL-003/CL-008で「下流の回答を上流に戻す手順が抜ける」再発を繰り返していたため、今回は解決と同時に上流を更新し、記録として残した |
| CL-011 | 2026-08-29 | `api-spec.yaml`(`MissRecordInput.expectedKey`・`SessionResult.consistency`のdescription)、`table-definition.md`(TBL-05 `expected_key`列の説明) | P3-06/P3-02で既に確定していた内容(expectedKeyのカンマ区切り規則、Consistencyの標準偏差定義)が、各文書の「未決事項」節は解決済みに更新されていたにもかかわらず、本文中のdescription・列説明が「未決事項」「暫定値」の文言のまま取り残されていた。ゲート③ doc-reviewer A7の指摘を受け該当箇所を確定内容に書き換えた | CL-003→CL-008に続く3度目の同型再発。未決事項を解決する際、文書末尾の「未決事項リスト」だけを更新し、本文中の該当description・列説明を検索して直す手順が運用に組み込まれていなかった |
| CL-012 | 2026-08-29 | `table-definition.md`(TBL-03 `reading`列を廃止し`mora_list`列を追加)、`api-spec.yaml`(`Sentence.reading`を`Sentence.moraList`に変更) | ゲート③ test-reviewer A4で、お題文(かな列)を拍(モーラ)単位に分解する処理がどの設計書にも存在しないと指摘された。Kazukiの発案により、実行時に分解ロジックを実装するのではなく、お題は開発者が投入する固定データである性質を活かし、シードデータ投入時点で拍列(`mora_list`)として保持する方式に変更した。分解ロジック自体が不要になり、実装コストも減った | P2(`table-definition.md`)執筆時点では「かな読み」を1つの文字列として持てば判定エンジンの入力として十分だと考えており、判定が拍(1〜複数文字のまとまり)単位で行われるためその境界情報自体をデータとして持つ必要がある、という点への検討が抜けていた |
| CL-013 | 2026-08-29 | `screen-design.md`(S-04に「もう一度送信」ボタンと保存失敗時の表示を追記) | ゲート③のA指摘修正(送信失敗時は記録を保持し再送する方式に変更)を`sequence.md`/`class-design.md`には反映したが、対応するUI要素をP2で確定済みの`screen-design.md`に反映し忘れていた。修正直後の検証レビュー(REV-013 A7)で指摘され、追記した | 同一セッション内で複数文書にまたがる変更を行った際、下流(P3)の文書は直接編集したが、UI要素の変更が上流(P2の`screen-design.md`)にも及ぶことへの意識が働かなかった。CL-003/CL-008/CL-011に続く同型の再発 |
| CL-014 | 2026-08-29 | `api-spec.yaml`(`POST /api/sessions`の400条件、`durationSeconds`を「1未満」→「負値(0は許容)」に緩和) | 新設した値域チェック(durationSeconds<1で400)が、`session-metrics.md`が既に前提としていた「制限時間モードで1打鍵も無いままセッションが保存されうる(durationSeconds=0もありうる)」と衝突していた。検証レビュー(REV-013 A5)で指摘され、0を許容するよう修正した | 値域チェック表を新設した際、既存のlogic-spec側の前提(0除算対応の既定値ロジック)と突き合わせずに「1以上」を機械的に設定してしまった |
| CL-015 | 2026-08-30 | `class-design.md`(1.4 に Service メソッドシグネチャ表を追加) | ゲート③ test-reviewer B1で、Service/Controllerのメソッド名・引数・戻り値が設計書に無くUTケースIDを「クラス×メソッド×条件」で採番できないと指摘されていた。P4-01(テスト計画作成)着手時に対応し、`api-spec.yaml`のoperationIdと1:1対応するメソッドシグネチャを確定した | ゲート③時点ではB(推奨)に分類され必須対応ではなかったため見送っていたが、P4着手時に「テストケースが書けるか」を実際に検証したところ即座に必要になった。B/C指摘の中でも「次工程の着手直後に必要になる」ものは、ゲート通過を優先せず先に潰しておく判断もありえた |
| CL-016 | 2026-08-30 | `api-spec.yaml`(`MissAnalysis.byKana`に`occurrenceCount`を追加)、`class-design.md`(`KanaMissStat`に`occurrenceCount`を追加)、`logic-spec/advice-generation.md`(カテゴリ2判定の逆算式を`対象.occurrenceCount`の直接参照に変更)、`db-access.md`(4章に出力への反映を追記) | ゲート③ doc-reviewer B9/ops-reviewer B8/test-reviewer C4で指摘されていた「`byKana`に出現回数が無く`missCount ÷ (missRate ÷ 100)`で逆算しており、丸め後の`missRate`が0になるとゼロ除算・精度劣化を起こす」問題。P4-03(UTケース作成)でUT-065として境界値ケースを書いた結果、未解決のまま残っていることが確認できた。Kazukiに相談し、`byCharType`(CL-007)と同じ`occurrenceCount`追加で解決する方針に決定した | `db-access.md`4章の時点で出現回数(分母)は`SessionKanaCountRepository`のクエリで既に取得・計算に使っていたが、それをAPI出力(`KanaMissStat`)にも含めるという発想が無く、率だけを返して呼び出し先(`AdviceGenerator`)に逆算させる設計のまま気づかれなかった。`byCharType`で同種の問題を一度解決済み(CL-007)だったにもかかわらず、`byKana`側への横展開がゲート③まで行われていなかった |
| CL-017 | 2026-09-06 | `test-plan.md`(3.1 `RA-001`ワークド例の`nextHint`を`"hi"`→`"i"`、`confirmedText`を`"し"`→`"shi"`に修正) | P5-04(`sequenceJudge.ts`実装)着手時、`romaji-automaton.md` 6.1の`nextHint`ルール(最短優先、REV-013 A2で「辞書順で先頭」から改訂済み)と`test-plan.md`のRA-001例が矛盾していることが判明した。「し」の候補`{si, shi}`で`s`まで入力済みの場合、最短優先なら`si`の残り`"i"`のはずだが、RA-001は`shi`の残り`"hi"`(辞書順で`shi`が`si`より先に来る、改訂前ルードの挙動)になっていた。さらに実装・テスト実行の過程で、`confirmedText`(定義上「採用パターンで表示」)も確定後の値がかな`"し"`のままで採用パターン`"shi"`になっていない誤りが同じ例の中に見つかった。Kazukiと相談し、最も新しく理由付きで改訂された`romaji-automaton.md`/`class-design.md`の明文化されたルール側を正としてRA-001を両方とも修正する方針で合意した | `romaji-automaton.md`のREV-013 A2改訂(2026-08-29)はP3中間レビュー中に行われたが、その時点で既に書かれていた`test-plan.md`は未着手(P4は2026-08-30以降)だった。P4-01(test-plan.md作成)でRA-001のワークド例を書いた際、`romaji-automaton.md`の改訂後の文章ではなく改訂前の記憶・慣習(辞書順、かな表示)で例を作ってしまい、突き合わせを怠った。`confirmedText`の誤りは`sequenceJudge.ts`を実装し実際にテストを流して初めて発覚しており、文書レビューだけでは検出できていなかった |
| CL-018 | 2026-09-06 | `class-design.md`(2.2に`Sentence.moraList`→`MoraSequence`変換の節を追加)、`types.ts`(`classifyCharType`関数を追加)、`sequenceJudge.ts`(`startSentence`の引数型を`MoraSequence`→`string[]`に変更し内部で分類) | P5-04(`sequenceJudge.ts`実装)着手時、`class-design.md`の「`MoraSequence`は`Sentence.moraList`をそのまま使う」という記述と、`api-spec.yaml`の`moraList`の実際の型(`charType`を持たないかな文字列の配列)が矛盾していることに気づいた。P5-03で実装済みの`Mora`型(`types.ts`)は判定分岐に`charType`を必須とするため、`string[]`→`Mora[]`への変換処理がどの設計書にも存在しなかった。Kazukiと相談し、`sequenceJudge`内部でこの変換を行う方式(呼び出し側であるTypingViewは何も変換しない)に決定した | `romaji-automaton.md`3章・CL-012(決定#8)は「読み文字列をモーラ境界で区切る処理(分解)」を実行時に持たないことは決めたが、「既に区切られた1つの拍の文字列を`charType`に分類する処理」は分解とは別の関心事であり、どの設計書(`class-design.md`/`romaji-automaton.md`/`api-spec.yaml`)にもこの変換の担当箇所を明記していなかった。「そのまま使う」という表現が、データ構造(配列の区切り方)とデータの型(charType付きオブジェクトか単なる文字列か)の違いを曖昧にしていた |
| CL-019 | 2026-09-07 | `class-design.md`(1.4 例外クラス一覧・例外→応答対応表に`InvalidRequestException`を追加) | P5-07(`UserController`/`UserService`)着手時、`UserService`の責務(「nameのバリデーション(trim後1〜100文字)」)と`api-spec.yaml`の`POST /api/users`が定義する400応答に対応する例外クラスが、`class-design.md`の例外一覧(`UserNotFoundException`/`TopicSetNotFoundException`/`InvalidSessionSubmissionException`の3つのみ)に存在しないことに気づいた。Kazukiと相談し、`InvalidSessionSubmissionException`(Session専用の値域チェック7項目に紐づく名前)を流用せず、単純な入力チェック用の汎用例外`InvalidRequestException`を新設する方針で合意した | `InvalidSessionSubmissionException`は`SessionService.submitSession`の値域チェック表(class-design.md 1.4)に合わせて命名した専用例外であり、他のServiceの単純なバリデーションまで面倒を見る汎用例外として設計されていなかった。P5-06までEntity/Repository/例外基盤の骨格を作る中で、UserServiceのような「存在確認以外の入力チェックを持つ最初のService」に実際に着手するまでこの抜けが表面化しなかった |
| CL-020 | 2026-09-07 | `class-design.md`(1.4 Serviceメソッドシグネチャ表: `UserService.identifyUser`の戻り値を`User`→`UserResponse`、`TopicSetService.listTopicSets`/`listSentences`を`List<TopicSet>`/`List<Sentence>`→`List<TopicSetResponse>`/`List<SentenceResponse>`に修正) | P5-07着手時、同じ1.4節内でメソッドシグネチャ表(User/TopicSetServiceはEntityを返す)と「Entity→DTO変換の責務」の一般原則(全ServiceがDTOに変換し、ControllerはDTO変換を行わない)が矛盾していることに気づいた。SessionService/MissAnalysisServiceは既にDTOを返す形になっており、User/TopicSetServiceだけ取り残されていた。Kazukiと相談し、一般原則の方に合わせてシグネチャ表を修正する方針で合意した | P4-01(test-reviewer B1対応)でメソッドシグネチャ表を確定した際、SessionService/MissAnalysisServiceの複雑な変換ロジック(4章のMissAnalysis組み立てと同型)を念頭に一般原則を書いたが、User/TopicSetServiceのような単純なfind-or-create・一覧取得もこの原則が及ぶことの確認・横展開を怠っていた |
| CL-021 | 2026-09-07 | `table-definition.md`(TBL-06 `char_type`列にCHECK制約を追記) | P5-05〜08完了時点で`/code-review`(標準スキル)による中間レビューを実施したところ、`miss_records.char_type`(TBL-05)にはCHECK制約があるのに、同じ値域を持つ`session_kana_counts.char_type`(TBL-06)にはCHECK制約が無いという非対称を指摘された。table-definition.md自体がこの非対称を持っていたと判明したため、まず設計書を修正し、`V1__create_schema.sql`は適用済みのため新規migration(`V2`)で制約を追加する方針にした | TBL-05とTBL-06は元々別々のタイミング(P2-04)で書かれ、どちらも`char_type VARCHAR(10)`という同じ列を持つが、CHECK制約を追記する際にTBL-05にしか適用せず、同じ値域を持つ列への横展開確認を怠っていた。CL-003/CL-008/CL-011と同型の「横展開漏れ」パターン |
| CL-022 | 2026-09-10 | `class-design.md`(2.2に`startSentence`の戻り値の節を追加)、`logic-spec/romaji-automaton.md`(6.1に「1打鍵もしていない時点のヒント」を追加) | P5-15(`TypingView`)着手時、`sequenceJudge.startSentence()`が`void`を返す(`KeystrokeResult`は`handleKeystroke`しか返さない)ため、お題文表示直後・1打鍵もしていない時点で`TypingView`が「次に打つべき文字」のヒントを取得する手段が設計上無いことに気づいた。Kazukiと相談し、`startSentence`が最初の拍の初期状態を表す`KeystrokeResult`(表示専用、`sessionStore`には渡さない)を返す方式に決定した | 6.0/6.1の疑似コードは「キー入力イベントのたびに1回呼ばれる」処理として書かれており、1打鍵目より前の状態(お題文が表示された直後の画面)は判定オートマトンの疑似コードの対象外だった。画面側(`TypingView`)の初期表示要件は`screen-design.md`/`class-design.md`のどちらにも明記が無く、実際に呼び出し側を実装する段階まで欠落に気づけなかった |
| CL-023 | 2026-09-11 | `class-design.md`(2.4に`sessionStore.startNewSentence()`アクションと採番リセットの節を追加)、`logic-spec/romaji-automaton.md`(7.1 `kanaOccurrenceNo`行に注記を追加) | P5-15〜17完了時点の中間コードレビュー(REV-014 A1)で、`moraIndex`が拍の**確定時**にしか増えないため、お題文の切れ目(前の文の最後の拍の確定直後〜次の文の最初の拍が未確定の間)では`moraIndex`が変化せず、`sessionStore.recordKeystroke`の変化検知だけでは次の文の最初のミスの`kanaOccurrenceNo`が誤って前の文の値を使い回すバグが見つかった。`sessionStore`に`startNewSentence()`を追加し`lastMoraIndex`をリセットする方式で解決した(`TypingView.beginSentence()`から呼ぶ) | `class-design.md` 2.2/`romaji-automaton.md` 7.1のkanaOccurrenceNo採番規則は「1セッション=1つの`MoraSequence`」を暗黙の前提に書かれており、複数文セッション(P5-13時点で既に実装されていた`sessionStore`が想定していたはずのユースケース)における`moraIndex`のお題文またぎの挙動(拍が確定するまで増えない)まで踏み込んで検証していなかった。単体テスト(P5-13)も1セッション1回の`startSentence`呼び出ししか再現しておらず、統合(P5-15で複数文セッションを実際に動かすコードが書かれた時点)で初めて顕在化した |
| CL-024 | 2026-09-11 | `class-design.md`(2.2に`sequenceJudge.isSentenceComplete()`の節を追加) | P5中間コードレビュー(REV-014 B1)で、`TypingView`がお題文完了を`KeystrokeResult.confirmedMora !== null`の発生回数を数える自前カウンタで判定しており、1キーで2拍が同時に確定する既知の制限(`romaji-automaton.md` 6.1)に該当するキー入力が発生するとカウントが1つ足りなくなり、`advance()`が呼ばれず次のキー入力で`handleKeystroke`が例外を投げてセッションが凍結する不具合が見つかった。`sequenceJudge`に`isSentenceComplete(): boolean`(内部の`indexInSequence`が唯一の正)を追加し、`TypingView`はこれを呼ぶ方式に変更した | P5-04(`sequenceJudge`実装)時点では「お題文が完了したことをどう呼び出し元に伝えるか」がどの設計書にも明記されておらず、P5-15(`TypingView`実装)で初めてこの手段が必要になった際、`class-design.md`2.2の公開インターフェース一覧(`startSentence`/`handleKeystroke`)に無い手段を実装側が独自に(`confirmedMora`のカウント)編み出してしまった。CL-022(`startSentence`の戻り値)と同型の「呼び出し元の要求を判定エンジン設計時に先取りできていなかった」パターン |
| CL-025 | 2026-09-11 | `db-access.md`(4.1 `SessionRepository`のメソッド名・戻り値をコードに追従)、`sequence.md`(2章 履歴一覧取得のメソッド呼び出しをコードに追従)、`class-design.md`(1.4 例外→応答対応表から到達不能な`MethodArgumentNotValidException`行を削除し、既存の`MethodArgumentTypeMismatchException`と新規の`MissingServletRequestParameterException`を追加) | P5-09〜17範囲(`4ff2106..fcd44ff`)を対象にした追加`/code-review`で、実装が既に変わっていたのに追従していなかった設計書2件が見つかった。①`SessionRepository.findByUserIdOrderByPlayedAtDesc`はP5-09実装時にN+1回避のため`findSummariesByUserIdOrderByPlayedAtDesc`(`SessionSummaryProjection`を返すJOINクエリ)に変更済みだったが、`db-access.md`/`sequence.md`は旧メソッド名のままだった。②`GlobalExceptionHandler`の`MethodArgumentNotValidException`ハンドラは2026-09-07(P5-05〜11中間レビュー対応)に「到達不能」として削除済みだったが、`class-design.md`の対応表への反映が漏れていた。同時に追加済みの`MethodArgumentTypeMismatchException`も対応表に載っていなかった。あわせて`MissingServletRequestParameterException`(`GET /api/users/{userId}/best`の`topicSetId`未指定で500になる新規バグ)への対応も設計に追加し、`GlobalExceptionHandler`にハンドラを実装した | ①はCL-003/CL-008/CL-011と同型の「実装側の変更を発生元の設計書へ戻す手順が運用に組み込まれていない」再発。②はP5-05〜11中間レビュー(2026-09-07)がその場でのバグ修正のみをコミットメッセージに残し、`class-design.md`の対応表という「他の設計書」への反映を範囲外にしていた。どちらも自分(同一セッションのClaude)がコードを修正した直後は気づけず、独立コンテキストの`/code-review`で初めて検出された |
| CL-026 | 2026-09-11 | `screen-design.md`(S-06に「ホームへ」→ S-02の遷移行を追加) | P5-18(router整備)着手時、S-06(ミス分析画面)だけ`screen-design.md`の3章に「遷移」の行が無く、`MissAnalysisView.vue`にも戻る手段が実装されていない(画面の行き止まり)ことに気づいた。S-05(履歴画面)が「ホームへ」ボタンを持つのと対称になるよう、S-06にも同じボタンを追加した | S-02ヘッダーから直接S-06へ遷移できる導線(2章)はP2-02で確定していたが、S-06自体の「戻る」導線は各画面詳細(3章)を1つずつ書く際に見落とされ、doc-reviewer等のレビューでも他画面との横並び比較(S-05にはあるがS-06には無い)という観点では拾われないまま残っていた |
| CL-027 | 2026-09-12 | `test-plan.md`(2.3節ST対象表のE2Eツール欄・6章未決事項を解決)、`st-cases.md`(未決事項を解決済みに更新) | P6-00a(未決事項解決)着手。「E2Eフレームワークの選定はP5着手時に行う」という2026-08-30時点の予定が、P5内では「E2E/Testcontainersは保留」として先送りされたまま未解決だった。Kazukiと相談し、実務での使用実績(Playwrightは実務でも代表的なE2Eツール)を踏まえ学習目的も兼ねてPlaywright導入で確定。NFR-01/02の計測方法も、既に2案併記されていた「ブラウザ開発者ツールのPerformanceタブ」「E2Eテスト内`performance.now()`」のうち後者(再現性が高い)に確定した | 「P5着手時に行う」という先送りが、具体的にいつ・誰が判断するかのトリガーを持たない曖昧な期限設定だった。P5-01着手時の「保留」判断で実質無期限延期になり、P5完了まで表面化しなかった。未決事項を先送りする際は「次にいつ判断するか」を明記しないと自然消滅するリスクがある |
| CL-028 | 2026-09-12 | `sequence.md`(6章未決事項を解決)、`frontend/src/api/errorHandling.ts`(新規、`handleUserNotFound`)、`HistoryView.vue`/`MissAnalysisView.vue`(catchブロックから呼び出し) | P6-04(ST-010着手)で、`sequence.md`5.2(userId失効時にuserStoreをクリアしS-01へ強制遷移する共通処理)が「物理的な実装配置はP5で決める」という未決事項のまま、P5完了時点でも実装されていなかったことが判明した。Kazukiと相談し、今回実装してからST-010を書く方針で合意。呼び出し元Viewの`useRouter()`結果を引数で渡す設計にし、モジュール側でrouterのシングルトンをimportしない(既存のテストでのrouter差し替えを壊さないため) | P5-05以降「backend/frontend含む全タスクをClaudeドラフト」に統一されて以降も、5.2は「P5で決める」という先送りのまま特定のP5タスクIDに割り当てられておらず、CL-027(E2Eフレームワーク選定)と同型の「担当タスクが無い未決事項は自然消滅する」パターンだった。P6でSTシナリオを実際に書く段階になって初めて、未実装であること自体に気づいた |
| CL-029 | 2026-09-12 | `sequence.md`(5.2に追記)、`frontend/src/stores/sessionStore.ts`(`submit`/`endSession`が`router`を受け取り内部で`handleUserNotFound`を呼ぶよう変更、戻り値を`boolean`化)、`ResultView.vue`/`TypingView.vue`(呼び出し側をtrue時は追加遷移しないよう変更) | P6-07(まとめ・完了確認)着手時、Kazukiの提案で「P6-04で追加した本番コード(`errorHandling.ts`周り)が一度もコードレビューされていない」ことに気づき`/code-review`(medium/high並列)を実施。`sequence.md`5.2の対象View一覧には元々S-04(ResultView)が明記されていたが、CL-028では`HistoryView`/`MissAnalysisView`(Viewから直接API呼び出し)にしか組み込まれておらず、S-04はAPI呼び出しが`sessionStore.submit()`経由(Piniaストア)という構造の違いから対応が漏れていた。userId失効時にResultViewの「もう一度送信」が404を繰り返す無限ループになる不具合を修正した。他に2件(`HistoryView`の同時404で`handleUserNotFound`が2重に呼ばれる件、`MissAnalysisView`のテスト未カバー)も検出された。前者は`bug-list.md`にBUG-002として起票し、実際に検証した結果vue-router 5では`router.push`が例外を投げず実害が無いことを確認したためコード修正は見送った(残課題として明記)。後者はテスト網羅性の指摘であり本体不具合ではないため`bug-list.md`への起票はせず、本記録にのみ残す | CL-028時点で「対象Viewからcatchブロック経由でAPIを呼ぶ」パターンだけを前提に実装し、S-04がストア経由という別パターンであることを`sequence.md`の対象View一覧と照合しなかった。CL-003/CL-008/CL-011/CL-021と同型の「横展開漏れ」パターンで、今回は独立コンテキストの`/code-review`で初めて検出された |
| CL-030 | 2026-09-12 | `frontend/src/api/errorHandling.ts`(`getTraceId`新設)、`sessionStore.ts`(`submitErrorTraceId`状態追加)、6View(`NameInputView`/`HomeView`/`TypingView`/`ResultView`/`HistoryView`/`MissAnalysisView`)のエラー表示に`traceId`を併記 | P7-04(operation.md作成)着手前、P3ゲート③ opsレビュー(`review-20260829_p3-gate3-ops.md` C3)の「エラー画面に`traceId`を小さく併記する」という指摘が「P5実装時の申し送りで足りる」とされたまま、`wbs.md`のB/C指摘振り分け表のどのフェーズにも割り当てられておらず、P5完了時点でも未実装だったことに気づいた。バックエンドは`GlobalExceptionHandler`で`traceId`を生成・返却済み、フロントエンドの型定義にも存在したが、どの画面のエラー表示にも実際には出力されていなかった。Kazukiと相談し、P7(公開URLを他人に渡す前提の工程)で今回実装する方針にした | CL-027/CL-028と同型の「担当タスクIDが割り当てられないまま先送りされた指摘は自然消滅する」パターンが3件目。今回はレビュー指摘表(`wbs.md`のB/C振り分け)自体に記載漏れがあり、後続フェーズのタスク分解時に拾われる仕組みが機能しなかった |
| CL-031 | 2026-09-12 | `MissAnalysisService.java`(missRate/accuracyRateを小数第2位HALF_UPで丸め)、`AdviceGenerator.java`/`MissAnalysisView.vue`(`expectedKey`のカンマ区切りを中黒「・」区切りに整形)、`sequence.md`(図2〜4にapi層参加者・図4にuserId存在確認ステップを追加、5.3にuserIdあり×S-01遷移の注記追加)、`romaji-automaton.md`(9章の古い分類用語を更新、7.1に`kanaOccurrenceNo`の「かな種類ごと」明記と`keystrokeIntervalsMs`の仕様注記を追加)、`class-design.md`(90秒タイムアウトの根拠訂正、1.5にFR-02/FR-03のトレーサビリティ行追加)、`db-access.md`(7章にR__シードの冪等性注意書き追加) | CL-030(ops-C3の転記漏れ)をきっかけに、`wbs.md`の「P3ゲート③検証レビュー(REV-013)のB4件・C7件はP5着手時の確認に追加する」という記述自体が、実際には一覧に転記されず放置されていたことが判明。Kazukiの提案で11件全てを監査し、Kazuki了承のうえ全件対応した。verify-B4(丸め未実装)とverify-B5(カンマ区切り表示)は実際の表示・判定に影響する実質的なギャップ、残りは`sequence.md`/`romaji-automaton.md`/`class-design.md`/`db-access.md`のドキュメント記述の齟齬・不足だった | CL-027/CL-028/CL-030と同型の「担当タスクIDが割り当てられないまま先送りされた指摘は自然消滅する」パターンで、`wbs.md`のB/C振り分け表の「追記」節(一覧本体とは別の文章)に書かれた項目は特に見落とされやすいと判明した。次回以降、新しい追記を行う際は文章で終わらせず、対応する一覧(「P{n}着手時に確認」等)へのその場での転記までを1セットにする |
